"use strict";
window.onload = function(){
var makePerson = function(persArr){
    
    var splitedArray, ageArray, nameArray;
    
    splitedArray = persArr.split(',');
    
    ageArray = new Array(splitedArray[2], splitedArray[4], splitedArray[6]);
    nameArray = new Array(splitedArray[1], splitedArray[3], splitedArray[5]);
    console.log(ageArray);
    
    var person;
	var names;
	var minAge;
	var maxAge;
	var averageAge;
	var personObj = {};
	
	
	//personObj.averageAge = 
	
	//var totalAgeSum = persons.map(function(persons){
    //return persons.age;
	});
    
};
};
